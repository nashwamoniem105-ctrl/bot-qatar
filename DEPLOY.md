# تعليمات النشر - نظام مخالفات قطر

تم تحويل النظام بالكامل ليعمل مع بوابة وزارة الداخلية القطرية (MOI Qatar).

## المتطلبات الأساسية
1. قاعدة بيانات PostgreSQL (عبر Drizzle ORM).
2. بيئة تشغيل Node.js 20+.
3. حساب على Railway أو أي منصة استضافة تدعم Node.js.

## متغيرات البيئة (Environment Variables)
يجب ضبط المتغيرات التالية في لوحة تحكم الاستضافة:

| المتغير | الوصف |
| --- | --- |
| `DATABASE_URL` | رابط الاتصال بقاعدة البيانات |
| `JWT_SECRET` | مفتاح سري لتشفير الجلسات |
| `ADMIN_PASSWORD` | كلمة مرور لوحة الإدارة (الافتراضية: admin123) |
| `NODE_ENV` | ضبط القيمة على `production` |

## خطوات النشر
1. قم برفع الكود إلى مستودع GitHub الخاص بك.
2. اربط المستودع بمنصة Railway.
3. أضف خدمة PostgreSQL واربطها بالتطبيق.
4. سيتم تشغيل عمليات الهجرة (Migrations) تلقائياً عند الإقلاع.

## المميزات الجديدة
- واجهة مستخدم مطابقة لموقع وزارة الداخلية القطرية.
- دعم الاستعلام برقم المركبة، الرقم الشخصي، وقيد المنشأة.
- بوابة دفع تحاكي بوابة الدفع القطرية الرسمية.
- لوحة تحكم إدارية لمتابعة الجلسات وتغيير حالات الدفع (OTP/ATM PIN).
